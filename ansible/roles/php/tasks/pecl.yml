- name: Install php5-dev
  apt: pkg="php5-dev" state=present

- name: Install PECL packages
  shell: echo "\n\n\n\n\n\n\n\n\n" | pecl install {{ item }}
  register: pecl_result
  changed_when: "'already installed' not in pecl_result.stdout"
  failed_when: "pecl_result.stderr or ('ERROR' in pecl_result.stdout)"
  with_items: php.pecl_packages

- name: Create PECL extension .ini file
  template: >
    src="extension.tpl"
    dest="/etc/php5/mods-available/{{ item }}.ini"
    owner="root"
    group="root"
    mode=0644
  with_items: php.pecl_packages

- name: Enable PECL extension
  shell: php5enmod {{ item }}
  with_items: php.pecl_packages
  changed_when: false
